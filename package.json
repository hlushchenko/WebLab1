{
  "name": "weblab1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "stylelint-config-prettier": "^9.0.3"
  },
  "devDependencies": {
    "eslint": "^5.1.0",
    "eslint-config-metarhia": "^7.0.1",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^5.2.0",
    "husky": "^7.0.0",
    "prettier": "2.5.1",
    "stylelint": "^14.2.0",
    "stylelint-config-standard": "^24.0.0"
  },
  "scripts": {
    "test": "npm run format-check && npm run eslint-check && npm run stylelint-check",
    "fix": "npm run format-fix && npm run eslint-fix && npm run stylelint-fix",
    "prepare": "husky install",
    "format-fix": "prettier --write . --ignore-path .gitignore",
    "format-check": "prettier --check . --ignore-path .gitignore",
    "eslint-fix": "eslint . --fix --ignore-path .gitignore",
    "eslint-check": "eslint . --ignore-path .gitignore",
    "stylelint-fix": "stylelint \"**/*.css\" --fix --ignore-path .gitignore",
    "stylelint-check": "stylelint \"**/*.css\" --ignore-path .gitignore"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hlushchenko/WebLab1.git"
  },
  "author": "Hlushchenko",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/hlushchenko/WebLab1/issues"
  },
  "homepage": "https://github.com/hlushchenko/WebLab1#readme"
}
